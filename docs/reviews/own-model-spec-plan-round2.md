**REVISE — findings 1 and 3 retain interface ambiguities before foundation implementation.** The remaining scoped findings are addressed as design requirements; this review makes no claim about implemented behavior.

1. **NOT ADDRESSED — form binding remains incomplete.** Website tools, namespaces, representation selection, document access, and MCP coverage requirements are now explicit. However, [`ApiRegistry.bind`](/home/bernt-popp/development/clinpgx-link/docs/superpowers/plans/2026-09-05-clinpgx-link.md:104) still accepts only path/query parameters and returns the original three-element tuple, while the client and service now accept forms. The addendum requires body validation but supplies no replacement binder signature. **Remaining correction:** define how validated form parameters pass through binding to the client, including rejection of unknown fields. The addendum’s precedence clause establishes required behavior but leaves this shared interface undecided.

2. **ADDRESSED — oversized content has a specified retrieval path.** The [large-content contract](/home/bernt-popp/development/clinpgx-link/docs/superpowers/specs/2026-09-05-source-access-contract.md:53) defines acquisition caps, operation-specific recovery, immutable content references, retention, pointer discovery, scalar slices, exact-byte reconstruction, and nonzero progress. Required tests cover the measured large scalar, nested arrays, oversized first rows, fencing overhead, and HTTP-cap fallback. Unresolved acquisition failures explicitly prevent coverage acceptance. No important remaining design ambiguity identified.

3. **NOT ADDRESSED — local discovery semantics are improved, but the joined-query contract remains underspecified.** The [local discovery section](/home/bernt-popp/development/clinpgx-link/docs/superpowers/specs/2026-09-05-source-access-contract.md:96) establishes aliases, identifiers, tokenized membership, join identities, broader source vocabularies, and component provenance. It still does not specify how callers discover the accepted local filter fields and membership semantics, or which public call returns joined annotation/evidence/allele components and in what shape. Saying memberships “feed” searches and relationships permits materially different implementations. **Remaining correction:** freeze the discovery mechanism for local filters/tokenizers and a concrete public query/result contract for membership and joined evidence; require MCP acceptance examples covering multi-gene/multi-drug cells, study/evidence joins, reverse rows, and Swissmedic/non-CPIC/DPWG sources.

4. **ADDRESSED — snapshot validation and reads are bound together.** The [snapshot contract](/home/bernt-popp/development/clinpgx-link/docs/superpowers/specs/2026-09-05-source-access-contract.md:118) requires identity validation, counting, and row selection on one immutable handle. Repository signatures now accept `expected_snapshot`, cursor decoding passes it through, and deliberately interleaved activation/rollback tests are required. No important remaining ambiguity identified within this finding.

9. **ADDRESSED — accounting cannot substitute for usable coverage.** The [coverage contract](/home/bernt-popp/development/clinpgx-link/docs/superpowers/specs/2026-09-05-source-access-contract.md:144) separates the gates, records six independent coverage dimensions, includes nonregistry sources, and keeps unavailable/corrupt sources incomplete unless a working equivalent exists. PDF signature checks explicitly cannot establish text coverage. [Task 6](/home/bernt-popp/development/clinpgx-link/docs/superpowers/plans/2026-09-05-clinpgx-link.md:291) carries these requirements into acceptance.

10. **ADDRESSED — benchmark acceptance is measurable.** The [benchmark contract](/home/bernt-popp/development/clinpgx-link/docs/superpowers/specs/2026-09-05-source-access-contract.md:158) requires a sourced, checked-in 18-case manifest before tuning; all cases completed; all deterministic data/provenance and no-fabrication assertions passing; and at least 90% of noncritical rubric points. It fixes per-case limits, separates reruns, retains unavailable cases, and requires actual model/client identities and HTTP MCP traces. Creating the manifest remains required implementation work, rather than evidence claimed by this design review.

**New important breakage introduced by the corrections:** none identified within the assigned scope. Release findings 5–8 were not assessed. No edits, subagents, implementation tests, or research-corpus re-review were performed.

**Model configuration:** GPT-6-based Codex, as identified by the session instructions. The exact serving model identifier, reasoning-effort setting, and client version are not exposed to this reviewer; they cannot be independently recorded as verified configuration.

SHA-256 values were unchanged between initial and final checks:

```text
1666246d71a84e38981bcd48b6a2f4c80d26d411ac7a8c5a14c77be7814ab455  docs/reviews/own-model-spec-plan-round1.md
663145b640e8a3f7856c0f1d9447e521bfd65a2bc0fd56057c23f31f8f38ad45  docs/superpowers/specs/2026-09-05-source-access-contract.md
564fed4aab98a45798036fa48bed3232536f2ff871286452d5a0c93590873a98  docs/superpowers/plans/2026-09-05-clinpgx-link.md
```
